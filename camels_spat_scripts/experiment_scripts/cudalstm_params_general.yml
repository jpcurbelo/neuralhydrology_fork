
seed: 1111
# --- Experiment configurations --------------------------------------------------------------------

# experiment name, used as folder name
# experiment_name: 426_camels_spat_USA
experiment_name: 10_camels_spat_USA

## file to specify training, validation and test basins (relative to code root or absolute path)
# basin_id_file: 426_basin_camels_spat_1975-2019_usa.txt
basin_id_file: 10_basin_camels_spat.txt

# training, validation and test time periods (format = 'dd/mm/yyyy')
# train_start_date: "01/10/1975"
# train_end_date: "30/09/1995"
# validation_start_date: "01/10/1995"
# validation_end_date: "30/09/2007"
# test_start_date: "01/10/2007"
# test_end_date: "30/09/2019"

train_start_date: "01/10/1980"
train_end_date: "30/09/2005"
validation_start_date: "01/10/2005"
validation_end_date: "30/09/2010"
test_start_date: "01/10/2010"
test_end_date: "30/09/2019"

# which GPU (id) to use [in format of cuda:0, cuda:1 etc, or cpu or None]
device: cuda:0

# --- Validation configuration ---------------------------------------------------------------------

# specify after how many epochs to perform validation
validate_every: 5

# specify how many random basins to use for validation
validate_n_random_basins: 5

# specify which metrics to calculate during validation (see neuralhydrology.evaluation.metrics)
# this can either be a list or a dictionary. If a dictionary is used, the inner keys must match the name of the
# target_variable specified below. Using dicts allows for different metrics per target variable.
metrics:
  - NSE
  - Alpha-NSE
  - Beta-NSE
  - FHV
  - FMS
  - FLV
  - KGE
  - Beta-KGE
  - Peak-Timing
  - Peak-MAPE
  - Pearson-r


# --- Model configuration --------------------------------------------------------------------------

# base model type [lstm, ealstm, cudalstm, embcudalstm, mtslstm]
# (has to match the if statement in modelzoo/__init__.py)
model: cudalstm

# prediction head [regression]. Define the head specific parameters below
head: regression

# ----> Regression settings <----
output_activation: linear

# ----> General settings <----
# # Number of cell states of the LSTM
# hidden_size: 256

# Initial bias value of the forget gate
initial_forget_bias: 3

# # # Dropout applied to the output of the LSTM
# # output_dropout: 0.4

# --- Training configuration -----------------------------------------------------------------------

# specify optimizer [Adam]
optimizer: Adam

# # # specify loss [MSE, NSE, RMSE]
# # # loss: MSE
# # loss: NSE

# # # specify learning rates to use starting at specific epochs (0 is the initial learning rate)
# # learning_rate:
# #   0: 1e-3
# #   20: 5e-4
# #   25: 1e-4

# # Mini-batch size
# batch_size: 256

# Number of training epochs
epochs: 5

# If a value, clips the gradients during training to that norm.
clip_gradient_norm: 1

# Defines which time steps are used to calculate the loss. Can't be larger than seq_length.
# If use_frequencies is used, this needs to be a dict mapping each frequency to a predict_last_n-value, else an int.
predict_last_n: 1

# # Length of the input sequence
# # If use_frequencies is used, this needs to be a dict mapping each frequency to a seq_length, else an int.
# seq_length: 270

# Number of parallel workers used in the data pipeline
num_workers: 16

# Log the training loss every n steps
log_interval: 2

# If true, writes logging results into tensorboard file
log_tensorboard: True

# If a value and greater than 0, logs n random basins as figures during validation
log_n_figures: 2

# Save model weights every n epochs
save_weights_every: 1


# --- Data configurations --------------------------------------------------------------------------

# which data set to use [camels_us, camels_gb, global, hourly_camels_us]
dataset: camels_spat

# Path to data set root
# data_dir: ../../../../../gladwell/hydrology/SUMMA/summa-ml-models/CAMELS_spat_NH
data_dir: ../../../DATA/CAMELS_spat_NH

dynamic_inputs:
# # # e,mper,msdwlwrf,msdwswrf,msnlwrf,msnswrf,mtpr,q,rh,sp,t,u,v,w,phi,tmean,prcp
- e           #Vapor pressure - Derived
- mper        #Mean potential evaporation rate - ERA5
- msdwlwrf    #Mean downward LONGtwave radiation at the surface - ERA5
- msdwswrf    #Mean downward SHORTwave radiation at the surface - ERA5
- msnlwrf     #Net LONGwave radiation at the surface - ERA5
- msnswrf     #Net SHORTwave radiation at the surface - ERA5
- mtpr        #Mean total precipitation rate - ERA5
- q           #Specific humidity - ERA5
- rh          #Relative humidity - Derived
- sp          #Surface pressure - Derived
- t           #Temperature - ERA5
- u           #U-component of wind - ERA5
- v           #V-component of wind - ERA5
- w           #Wind speed (mean) - Derived
- phi         #Wind direction - Derived
- tmean       #Temperature - EM-Earth
- prcp        #Mean total precipitation rate - EM-Earth

# which columns to use as target
target_variables:
  - q_obs

# clip negative predictions to zero for all variables listed below. Should be a list, even for single variables.
clip_targets_to_zero:
  - q_obs